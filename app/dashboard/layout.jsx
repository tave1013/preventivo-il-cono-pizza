import './globals-dash.css';

export const metadata = {
  title: 'Dashboard — Il Cono Pizza di Busto Arsizio',
  description: 'Pannello di gestione marketing digitale',
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
